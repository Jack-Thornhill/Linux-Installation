import React from 'react'

// Add in audio on page navigation

export default class WindowsStart extends React.Component {
  render() {
    return (
      <div>
        <h1>Why are we still here, just to suffer?</h1>
      </div>
    )
  }
}
